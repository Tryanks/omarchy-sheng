// Authenticate the active administrator, not another wheel member inherited
// from the ALARM base image. This selects identity; it does not grant access.
polkit.addAdminRule(function(action, subject) {
    if (subject.local && subject.active && subject.isInGroup("wheel")) {
        return ["unix-user:" + subject.user];
    }
});
